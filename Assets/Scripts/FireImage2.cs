using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class FireImage2 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(this.gameObject, 0.1f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
